var mongojs = require('./');
var db = mongojs('mongodb://localhost/test?ssl=true', ['a']);

db.on('error', function(err) {
  console.log('we had an error.', err);
});

db.a.find(function(err, dta) {
	console.log(err);
	console.log(dta);
});
